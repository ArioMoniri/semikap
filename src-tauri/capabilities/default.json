{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the TAMIAS desktop window. Keeps the surface minimal — the app does its work entirely inside the embedded WebView.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
