apiVersion: v1
kind: Service
metadata:
  name: {{ include "tamias.fullname" . }}
  labels:
    {{- include "tamias.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tamias.selectorLabels" . | nindent 4 }}
