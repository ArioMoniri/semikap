{
  "note": "Jobs for .github/workflows/benchmark.yml. 'cases' (optional) limits a job to those case ids \u2014 used to shard slow models (nnU-Net on MSD takes ~53 min per case on a 4-core runner). Results are merged into the benchmark-results-v1 release (dedupe by model sha256 + dataset + case). Current run: nnU-Net liver_22 only (previous shard ran out of memory; fixed by the rolling-window sliding inference).",
  "include": [
    {
      "model": "nnunet_liver_lits",
      "dataset": "msd_task03_liver",
      "cases": "liver_22"
    }
  ]
}
